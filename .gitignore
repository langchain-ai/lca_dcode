__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Local database — rebuild it with `make seed`
*.db
*.db-journal

# Local agent config that shouldn't be shared
.mcp.json
.deepagents/.mcp.json
.env
